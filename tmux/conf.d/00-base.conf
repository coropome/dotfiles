# Core session behavior and numbering.

set -g prefix C-a
bind-key C-a send-prefix

# Keep tmux-default docs and muscle memory usable.
set -g prefix2 C-b

set -sg escape-time 0

set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
